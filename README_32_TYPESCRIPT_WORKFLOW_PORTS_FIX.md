# Hotfix 32 — Workflow port typing

Corrige la normalización de los puertos de entrada de pasos Workflow para aceptar bindings con `module_input_key` opcional y conexiones mediante `source_path`.

No requiere cambios en Backend ni migraciones.
