# 14 · Cierre y validación final de BackOffice

Este incremento conserva todos los módulos existentes y añade una validación reproducible de estructura, lint y compilación de producción.

## Ejecutar

```powershell
npm install
npm run release:check
```

No se incluyen dependencias, `.next`, secretos ni archivos de entorno.
