# Hotfix 31 - React Flow dependency

This hotfix moves the React Flow global stylesheet import to the root App Router layout.

After extracting over the BackOffice root, install the exact dependency before building:

npm install @xyflow/react@12.11.2

Then clean and build normally.
