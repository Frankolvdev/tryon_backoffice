export interface CashboxSummary { collected_usd:number; available_usd:number; protected_infrastructure_usd:number; blocked_profit_usd:number; released_commercial_profit_usd:number; rounding_and_operational_surplus_usd:number; expiration_releases_usd:number; withdrawals_usd:number; active_bags:number; new_bags:number; expired_bags:number }
export interface TokenBag { id:number; user_id:number; user_email?:string|null; source:string; source_label:string; reference_id?:string|null; status:string; original_tokens:number; remaining_tokens:number; consumed_tokens:number; amount_paid_usd:number; effective_token_value_usd:number; normal_profit_per_token_usd:number; effective_profit_per_token_usd:number; infrastructure_capacity_per_token_usd:number; commercial_profit_total_usd:number; commercial_profit_released_usd:number; protected_infrastructure_remaining_usd:number; infrastructure_used_usd:number; rounding_surplus_usd:number; expiration_release_usd:number; coupon_code?:string|null; plan_name?:string|null; payment_status?:string|null; refundable:boolean; refund_reason:string; activated_at?:string|null; expires_at?:string|null; expired_at?:string|null; created_at:string }
export interface TokenBagList {items:TokenBag[];total:number}
export interface BagGeneration {execution_id:string;tokens_used:number;created_at:string;infrastructure_cost_usd:number;company_profit_usd:number;rounding_surplus_usd:number;status?:string|null}
export interface BagDetail {bag:TokenBag;generations:BagGeneration[];timeline:{type:string;at:string;label:string}[];purchase_id?:number|null}
export interface Withdrawal {id:number;amount_usd:number;currency:string;beneficiary?:string|null;concept:string;method?:string|null;proof_url?:string|null;notes?:string|null;created_by_user_id?:number|null;withdrawn_at:string;created_at:string}
export interface ExpirationSettings {enabled:boolean;days:number}
