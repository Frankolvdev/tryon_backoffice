# MegaZIP BackOffice — detalle financiero fácil de entender

Actualiza la vista existente de Finanzas sin eliminar su tabla:

- Agrega la columna azul **Total de la generación**.
- Agrupa visualmente cada bolsa de tokens.
- Separa ganancia después de beneficios y centavos por redondeo.
- Explica tiempo real, scaledown y margen técnico en una tabla.
- Muestra costo protegido del proveedor y total de la generación.

```powershell
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
npm run build
npm run dev
```
